# Scenario contract — sourced by bench.sh. Shell assignments only.
TOOLS=write          # turn 1 must be able to Edit companion-state.md
MULTITURN=1          # PROMPT.txt then PROMPT2.txt via `claude -p --continue`
STATE_FILE=companion-state.md   # mechanical grep target (checked after turn 1)
