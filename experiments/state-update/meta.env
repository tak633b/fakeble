# Scenario contract — sourced by bench.sh. Shell assignments only.
TOOLS=write          # the answer should update companion-state.md in the same turn
MULTITURN=0
STATE_FILE=companion-state.md   # mechanical grep target (checked after the run)
